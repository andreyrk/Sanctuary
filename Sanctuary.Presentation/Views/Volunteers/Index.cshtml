@{
    ViewBag.Title = "Voluntários";
}

<div class="flex flex-col py-8 px-4 sm:px-2">
    <div class="w-full max-w-screen-md mx-auto mb-8">
        <div class="flex mb-2 h-10">
            <span class="leading-loose text-xl font-bold">Voluntários</span>
        </div>
        <div class="flex flex-col">
            <v-data-table 
                :headers="entitiesHeaders" :items="entities" 
                :items-per-page="25" class="elevation-1" 
                :loading="!entitiesLoaded" loading-text="Carregando..." 
                hide-default-footer disable-sort>
                <template v-slot:item.birthdate="{ item }">
                    {{ new Date(item.birthdate).toLocaleDateString() }}
                </template>
            </v-data-table>
        </div>
    </div>
</div>

@section Script
{
    <script src="~/Volunteers/Volunteers.js" asp-append-version="true"></script>
}

@section Style {
    <link rel="stylesheet" href="~/Volunteers/Volunteers.css" asp-append-version="true" />
}